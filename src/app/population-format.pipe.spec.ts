import { PopulationFormatPipe } from './population-format.pipe';

describe('PopulationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
